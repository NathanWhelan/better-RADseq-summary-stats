% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adegenet.R
\name{as_genind}
\alias{as_genind}
\alias{as_genlight}
\title{Convert to an adegenet genind or genlight object}
\usage{
as_genind(H, popmap = NULL, verbose = TRUE)

as_genlight(H, popmap = NULL, drop_multiallelic = FALSE, verbose = TRUE)
}
\arguments{
\item{H}{A VCF path, or the object returned by \code{\link[=read_stacks_vcf]{read_stacks_vcf()}}
(optionally filtered).}

\item{popmap}{Optional: a popmap file path or the list returned by
\code{\link[=read_popmap]{read_popmap()}}, to set each individual's population.}

\item{verbose}{Print what was left out. Default \code{TRUE}.}

\item{drop_multiallelic}{If \code{FALSE} (default), a record with more than two
observed alleles stops \code{as_genlight()}. If \code{TRUE}, such records are left
out, with a message.}
}
\value{
\code{as_genind()}: an adegenet \code{genind} object. \code{as_genlight()}: an
adegenet \code{genlight} object.
}
\description{
Turns the data (a VCF path, or the object from \code{\link[=read_stacks_vcf]{read_stacks_vcf()}},
optionally filtered) into an object of the adegenet package, so it can be
used with adegenet, dartR, poppr and other packages that read these classes.
}
\details{
\itemize{
\item \code{as_genind()} works for any marker, including multi-allelic RAD
haplotypes from \code{populations.haps.vcf}. Each allele is named by its
sequence. It stores one number per individual and allele, so for a large
SNP dataset \code{as_genlight()} uses much less memory.
\item \code{as_genlight()} is for biallelic records (SNPs). Each genotype is stored
as the number of copies of the record's second allele (the ALT allele of a
normal SNP): 0, 1 or 2. For a normal SNP the second allele is always the
ALT, even if only one allele is seen, so objects made from different
subsets of samples agree. A record with more than two observed alleles
stops the function unless \code{drop_multiallelic = TRUE}, as in
\code{\link[=write_plink]{write_plink()}}.
}

Both leave out records with no genotyped individual, and write a missing
genotype as \code{NA}.

\strong{Locus names.} adegenet does not allow a \code{.} in a genind locus name, and
this package names the SNPs of one RAD locus \code{1}, \code{1.1}, \code{1.2}, ... So in
\code{as_genind()} every \code{.} becomes \verb{_} (\verb{1_1}, \verb{1_2}). The original names, and
each record's CHROM and POS, are kept in \code{other(x)}. \code{as_genlight()} keeps
the names as they are, and also sets \code{chromosome()} and \code{position()}, and
\code{alleles()} when every allele is a single base.

\strong{Which samples.} Every sample in the data is converted. With \code{popmap},
each sample gets its population (\code{pop()}), and every sample must be in the
popmap; remove the others first with \code{\link[=filter_samples]{filter_samples()}}.
}
\examples{
if (requireNamespace("adegenet", quietly = TRUE)) {
  popmap <- system.file("extdata", "small_popmap.tsv", package = "RADdiversity")
  haps <- read_stacks_vcf(system.file("extdata", "small.haps.vcf",
                                      package = "RADdiversity"), verbose = FALSE)
  gi <- as_genind(haps, popmap)          # haplotype loci, many alleles
  gi
  snps <- read_stacks_vcf(system.file("extdata", "small.snps.vcf",
                                      package = "RADdiversity"), verbose = FALSE)
  gl <- as_genlight(snps, popmap)        # SNPs as 0/1/2
  gl
}
}
\references{
Jombart, T. (2008) adegenet: a R package for the multivariate analysis of
genetic markers. \emph{Bioinformatics} 24:1403-1405.

Jombart, T. & Ahmed, I. (2011) adegenet 1.3-1: new tools for the analysis of
genome-wide SNP data. \emph{Bioinformatics} 27:3070-3071.
}
