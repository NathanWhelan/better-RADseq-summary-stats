% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differentiation_stats.R
\name{differentiation_stats}
\alias{differentiation_stats}
\alias{print.raddiv_differentiation}
\alias{summary.raddiv_differentiation}
\title{Population differentiation: FST, Weir & Goudet's beta, and Jost's D}
\usage{
differentiation_stats(
  vcf,
  popmap,
  nboot = 10000L,
  beta = TRUE,
  hierfstat_check = FALSE,
  outdir = NULL,
  stem = NULL,
  seed = NULL,
  verbose = TRUE
)

\method{print}{raddiv_differentiation}(x, ...)

\method{summary}{raddiv_differentiation}(object, details = FALSE, ...)
}
\arguments{
\item{vcf}{Path to \code{populations.snps.vcf} or \code{populations.haps.vcf}
(optionally gzip-compressed), or the object returned by
\code{\link[=read_stacks_vcf]{read_stacks_vcf()}}, optionally passed through \verb{filter_*()} functions.}

\item{popmap}{Path to a two-column, no-header popmap file (\verb{sample_id <TAB> population}), or the list returned by \code{\link[=read_popmap]{read_popmap()}}.}

\item{nboot}{Bootstrap replicates over RAD loci. Default \code{10000}; \code{0} for
none (the jackknife standard errors are always computed).}

\item{beta}{Also compute Weir & Goudet's pairwise beta with
\code{hierfstat::pairwise.betas()}, when hierfstat is installed. Default
\code{TRUE}. On large datasets this one call takes most of the run time (about
95\% at 100,000 SNPs); set \code{FALSE} to skip it. FST and D do not need it.}

\item{hierfstat_check}{If \code{TRUE} and \code{hierfstat} is installed, also run
\code{hierfstat::wc()} and report how far its global FST is from this
package's. Default \code{FALSE} (the package tests make this comparison, and
\code{wc()} is slow on large datasets).}

\item{outdir}{Directory to write the result tables to as TSV files
(created if needed). Default \code{NULL}: write nothing.}

\item{stem}{Text in the output file names
(\verb{diversity_per_population.<stem>.tsv}). By default it comes from the
VCF's file name (\code{populations.haps.vcf} gives \code{"haps"}), so runs on the
haplotype and SNP VCFs can share \code{outdir} without overwriting each other.
Required when writing files from an already-read object, which has no
file name.}

\item{seed}{Random seed for the bootstrap. Default \code{NULL}: use R's current
random-number stream, so call \code{set.seed()} first for reproducible
intervals. A number makes the result reproducible on its own and leaves
your session's random-number stream as it was.}

\item{verbose}{Print progress messages. Default \code{TRUE}.}

\item{x, object}{A \code{raddiv_differentiation} object, as returned by
\code{differentiation_stats()}.}

\item{...}{Ignored.}

\item{details}{For \code{summary()}: \code{FALSE} (the default) prints a short view --
FST, D and beta as \code{estimate (SE)}, what to report, and a list of checks.
\code{TRUE} prints the full report: every uncertainty column, the explanations
and the notes on missing data. Either way, the object returned holds the
table the short view prints, in \verb{$tables$results} (see "Tables for a
paper").}
}
\value{
An object of class \code{raddiv_differentiation}, a list of:
\describe{
\item{global}{One row: \code{FST}, \code{FIS} and \code{D}, each with jackknife SE
(\verb{_se}) and 95\% bootstrap interval (\verb{_lo}, \verb{_hi}), and \code{D_records},
the number of records the global D uses: only those typed in every
population (see Details).}
\item{pairwise}{One row per pair of populations: FST, beta and D, with
standard errors and bootstrap intervals. Each pair's D uses the
records typed in both of its populations.}
\item{pairwise_fst, pairwise_beta, pairwise_D}{The pairwise point
estimates as symmetric matrices (\code{NA} on the diagonal).
\code{pairwise_beta} is \code{NULL} when hierfstat is not installed or
\code{beta = FALSE}.}
\item{settings}{The settings of this run.}
}
Values are stored at full precision. With \code{outdir}, \code{global} and
\code{pairwise} are written to \verb{differentiation_global.<stem>.tsv} and
\verb{differentiation_pairwise.<stem>.tsv}.
}
\description{
Computes how much populations differ from each other (as opposed to
\code{\link[=diversity_stats]{diversity_stats()}}, which asks how variable each population is). Reports
Weir & Cockerham's (1984) FST and FIS and Jost's (2008) D, for all
populations together (global) and for each pair, and, when the \code{hierfstat}
package is installed, Weir & Goudet's (2017) pairwise beta. Printing the
result shows the tables. \code{summary()} shows FST and D as \code{estimate (SE)}, what
to report and a list of checks; \code{summary(details = TRUE)} adds every
uncertainty column and the notes on interpretation.
}
\details{
\strong{Why report both FST and D.} FST's ceiling shrinks as a locus's own
diversity grows, so two datasets with identical population structure can
show very different FST just because one used more variable markers (Jost
2008). RAD haplotype loci are such markers, so on a haplotype VCF prefer D
(or beta) for comparing datasets.

\strong{Bootstrap and jackknife.} Confidence intervals come from this package's
bootstrap over whole RAD loci, not from \code{hierfstat::boot.ppfst()}, which
resamples SNP rows independently and so ignores the linkage between SNPs
on one RAD tag. The FST/FIS formula is Weir & Cockerham's (1984) published
estimator; the package tests check it against \code{hierfstat::wc()}.

\strong{Records used for FST.} FST (global and pairwise) and the global FIS use a
record only where at least two of the compared populations have a
genotyped individual. A record typed in only one of them carries no
information about differences between populations, but including it
would add its within-population variance to FST's denominator and pull
FST toward 0. Such records are common when a Stacks run has 3 or more
populations and \code{-p} below their number. VCFtools and Stacks' own
pairwise FST skip them too; \code{hierfstat::wc()} and \code{pairwise.WCfst()} do
not, so on such data their FST is lower than this function's. Records
where every typed population has a single genotyped individual are also
skipped for FST and FIS, because no variance among individuals can be
estimated there. Missing individuals within a typed population are
handled by the estimator itself and are not affected. The number of
skipped records is in \code{settings$fst_records_skipped}.

\strong{Records used for global D.} The global D uses ONLY records typed in
every population; each pairwise D uses the records typed in both
populations of its pair. Jost's D for k populations measures
differentiation among those k populations, and a record missing some of
them measures it among a different set: averaging such records in biased
the global D low (0.875 instead of 1 for three completely differentiated
populations, one of them untyped at half the records). The global table's
\code{D_records} column, \code{print()} and \code{summary()} give the number of records
used. With 3 or more populations and much missing data this can be a
small share of the records; report pairwise D as well. If no record is
typed in every population, the global D is \code{NA} with a warning.

\strong{Combining loci for D.} Hs and Ht are averaged over loci and one D is
computed from the averages (mmod's \code{global.het}), not averaged over
per-locus D values. Hs and Ht are Nei & Chesser's (1983) estimators, which
use observed heterozygosity and so stay unbiased when individuals are
inbred; this is how \code{hierfstat::basic.stats()} computes its \code{Dest}. (The
mmod package uses a 2N/(2N - 1) correction that assumes random mating, the
same assumption behind Stacks' \code{Pi}, and gives a slightly higher D when
individuals are inbred.) A record where every typed population has a single
genotyped individual has no D.

\strong{References.} Weir, B.S. & Cockerham, C.C. (1984) Estimating
F-statistics for the analysis of population structure. \emph{Evolution}
38:1358-1370. \doi{10.1111/j.1558-5646.1984.tb05657.x} --
Weir, B.S. & Goudet, J. (2017) A unified characterization of population
structure and relatedness. \emph{Genetics} 206:2085-2103.
\doi{10.1534/genetics.116.198424} -- Jost, L. (2008) GST and
its relatives do not measure differentiation. \emph{Molecular Ecology}
17:4015-4026. \doi{10.1111/j.1365-294X.2008.03887.x} --
Winter, D.J. (2012) mmod: an R library for the calculation of population
differentiation statistics. \emph{Molecular Ecology Resources} 12:1158-1160.
\doi{10.1111/j.1755-0998.2012.03174.x} --
Goudet, J. (2005) HIERFSTAT, a package for R to compute and test
hierarchical F-statistics. \emph{Molecular Ecology Notes} 5:184-186.
}
\section{Tables for a paper}{

Put in the table exactly what the short \code{summary()} reports.
\code{summary(x)$tables$results} is that table as a data frame: one row for every
pair of populations (and an "all populations" row when there are 3 or more),
with \code{FST (SE)} and \code{D (SE)} as \code{estimate (SE)} text and \code{beta} as a plain
number (it is a point estimate and has no SE). The FST and D standard errors
are jackknifes over RAD loci; they count RAD loci only, and no simulation has
checked how often their 95\% intervals hold the true value, so the summary
says so. The other uncertainty columns (\verb{_lo}, \verb{_hi}) are in \code{global} and
\code{pairwise}, for a supplement. On a haplotype VCF report D (or beta) with FST.
}

\examples{
vcf    <- system.file("extdata", "small.haps.vcf", package = "RADdiversity")
popmap <- system.file("extdata", "small_popmap.tsv", package = "RADdiversity")
set.seed(1)
res <- differentiation_stats(vcf, popmap, nboot = 100, verbose = FALSE)
res
res$global$D
summary(res)$tables$results     # the table to put in a paper
}
