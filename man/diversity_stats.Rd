% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diversity_stats.R
\name{diversity_stats}
\alias{diversity_stats}
\title{Diversity statistics from a Stacks VCF}
\usage{
diversity_stats(
  vcf_file,
  popmap_f,
  g,
  nboot = 10000L,
  boot = "loci",
  sites = 0,
  min_n = 2L,
  complete_case = FALSE,
  outdir = ".",
  seed = 2024,
  verbose = TRUE
)
}
\arguments{
\item{vcf_file}{Path to \code{populations.snps.vcf} or \code{populations.haps.vcf}
(optionally gzip-compressed).}

\item{popmap_f}{Path to a two-column, no-header popmap TSV (\verb{sample_id <TAB> population}).}

\item{g}{Rarefaction size in GENE COPIES (10 diploids = 20). Required, no
default -- must be \verb{<= 2x} the smallest population.}

\item{nboot}{Bootstrap replicates. Default \code{10000L}; \code{0} = none.}

\item{boot}{Which axis the bootstrap resamples: one of \code{"loci"} (RAD loci,
the default -- matches hierfstat), \code{"individuals"} (within each
population, matches diveRsity), or \code{"both"} (Owen & Eckles 2012
crossed-factor scheme) -- matched exactly, not partially. \code{"individuals"}/
\code{"both"} are comparison modes only -- an empirical coverage simulation
found they badly undercover the true value for every statistic with a
finite-sample correction; see \code{README.md}, "Bootstrap mode".}

\item{sites}{Total sequenced sites -- the \code{Sites} column of the "All
positions (variant and fixed)" block of
\code{populations.sumstats_summary.tsv}. Adds per-sequenced-site Ho and He.
Default \code{0} (per-record values only).}

\item{min_n}{Available-data mode only: minimum typed individuals a
population needs at a locus to use that locus for that population.
Default \code{2L}, the mathematical floor below which Hs/FIS are undefined.
Ignored when \code{complete_case = TRUE}.}

\item{complete_case}{Use the old, stricter rule: a record is used only if
every individual of every population is genotyped there (Schmidt et al.
2021 recommendation (b)). Default \code{FALSE}.}

\item{outdir}{Directory to write the two output TSVs into. Default \code{"."}
(the current directory), created if it does not exist.}

\item{seed}{Random seed set before bootstrapping, for reproducibility.
Default \code{2024}. The caller's own RNG state (e.g. from their own prior
\code{set.seed()}) is restored when this function returns, so calling it
interactively does not affect subsequent random draws in the caller's
session.}

\item{verbose}{Print the VCF/popmap parsing summary. Default \code{TRUE}. The
main report (point estimates, CIs, "TAKE FROM THIS RUN" guidance) is
always printed regardless of this setting, matching the command-line
script.}
}
\value{
Invisibly, a list with elements \code{per_population} and \code{richness}
(the same two data frames written to \code{diversity_per_population.*.tsv}
and \code{diversity_richness.*.tsv}).
}
\description{
Computes Ho, He, FIS (Nei & Chesser 1983, ratio of sums), rarefied allelic
richness and rarefied private allelic richness from a Stacks 2
\code{populations} VCF and popmap, with a block bootstrap over RAD loci and a
delete-one-block jackknife standard error. Prints the same report as the
\code{diversity_stats.R} command-line script, writes the same two TSV files,
and additionally returns the result tables as data frames.
}
\details{
Run it twice: \code{populations.snps.vcf} gives Ho, He, \code{pct_poly} and the
per-sequenced-site values; \code{populations.haps.vcf} gives FIS, Ar and
privAr. Each run prints which of its numbers to take. See \code{README.md}
("Which file for which statistic") for the full reasoning.
}
