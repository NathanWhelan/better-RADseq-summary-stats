% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diversity_stats.R
\name{diversity_stats}
\alias{diversity_stats}
\alias{print.raddiv_diversity}
\alias{summary.raddiv_diversity}
\title{Diversity statistics from a Stacks VCF}
\usage{
diversity_stats(
  vcf,
  popmap,
  g,
  nboot = 10000L,
  boot = "loci",
  sites = NULL,
  min_n = 2L,
  complete_case = FALSE,
  se_individuals = TRUE,
  hierfstat_check = FALSE,
  outdir = NULL,
  stem = NULL,
  seed = NULL,
  verbose = TRUE
)

\method{print}{raddiv_diversity}(x, ...)

\method{summary}{raddiv_diversity}(object, details = FALSE, ...)
}
\arguments{
\item{vcf}{Path to \code{populations.snps.vcf} or \code{populations.haps.vcf}
(optionally gzip-compressed), or the object returned by
\code{\link[=read_stacks_vcf]{read_stacks_vcf()}}, optionally passed through \verb{filter_*()} functions.}

\item{popmap}{Path to a two-column, no-header popmap file (\verb{sample_id <TAB> population}), or the list returned by \code{\link[=read_popmap]{read_popmap()}}.}

\item{g}{Rarefaction size in GENE COPIES (10 diploid individuals = 20).
Required; at most twice the size of the smallest population.}

\item{nboot}{Bootstrap replicates. Default \code{10000}; \code{0} for none (the
jackknife standard errors are always computed).}

\item{boot}{What the bootstrap resamples: \code{"loci"} (whole RAD loci, the
default), \code{"individuals"} (within each population, as diveRsity does) or
\code{"both"} (Owen & Eckles 2012). Matched exactly. \code{"individuals"} and
\code{"both"} are for comparison only: in a coverage simulation their
intervals badly missed the true He and FIS (\code{vignette("rationale")},
"Why not bootstrap individuals?"). For uncertainty over individuals use
\code{se_individuals} (on by default) instead (see "How standard errors are
calculated" below).}

\item{sites}{Number of sequenced sites, to turn Ho and He into per-site
values (\code{Ho_autosomal}, \code{He_autosomal}; SNP VCF only). Default \code{NULL}: no
per-site values. Give one of:
\itemize{
\item the path to Stacks' \code{populations.sumstats_summary.tsv}: each
population's own \code{Sites} (from the "All positions (variant and fixed)"
block) is read with \code{\link[=read_sumstats_summary]{read_sumstats_summary()}};
\item one number, used for every population;
\item a named numeric vector, one value per population (names as in the
popmap);
\item a data frame with a \code{population} (or \code{pop}) column and a \code{sites} column.
}

\strong{How per-site values are calculated.} For each population, He summed
over the SNP records in the data, divided by \code{sites}. (In practice: the
mean He over the records the population uses, times the number of records
where it has at least one genotyped individual, shown as
\code{variant_records}.) Fixed sites add nothing to the sum but count in
\code{sites}. Ho is done the same way.

\strong{What \code{sites} must count.} Every sequenced site, variant or fixed, of
the RAD loci in the data, counting a site for a population only where at
least one of its individuals is genotyped there. This is how Stacks
counts \code{Sites}. Use the same popmap that was given to Stacks.

\strong{When Stacks' \code{Sites} is right.} Only for the data exactly as Stacks'
\code{populations} wrote them. Filtering afterwards (in R or with another
program) changes things:
\itemize{
\item SNPs removed, for example by \code{\link[=filter_mac]{filter_mac()}}: their He drops out of the
sum, so per-site values go down. For MAC and MAF filters this is real
rare-variant diversity being lost, as with Stacks' own \code{--min-mac};
report the threshold (\code{vignette("rationale")}, section 1). A RAD locus
whose every SNP was removed this way was still sequenced, so its sites
stay in \code{sites}.
\item Whole RAD loci removed as unreliable (low call rate, paralogs): their
sites should leave \code{sites} too, but Stacks' \code{Sites} still counts them,
so per-site values come out too low. Give \code{sites} for the loci you kept,
or use \code{\link[=pi_allsites]{pi_allsites()}} on an all-sites VCF.
\item A VCF thinned to one SNP per RAD locus (Stacks' \code{--write-single-snp},
\code{\link[=filter_thin_one_snp]{filter_thin_one_snp()}}) no longer holds all the SNPs. Compute per-site
values from the unthinned SNP VCF.
}

A warning is given when records or whole loci were removed after reading
(the \verb{filter_*()} functions record which filter removed them, so loci
emptied by \code{\link[=filter_maf]{filter_maf()}} or \code{\link[=filter_mac]{filter_mac()}} give no loci warning), or when
the VCF's SNP count does not match \code{Variant_Sites} in the Stacks file.}

\item{min_n}{Minimum genotyped individuals a population needs at a record
to use that record. Default \code{2}, the smallest number for which He and
FIS are defined. Ignored when \code{complete_case = TRUE}.}

\item{complete_case}{Use a record only if every individual of every
population is genotyped there (Schmidt et al. 2021, recommendation (b)).
Default \code{FALSE}.}

\item{se_individuals}{Also compute the uncertainty from which individuals
were sampled, and add two columns each for Ho, He and FIS (and their
per-site values): \verb{_se_ind} (a jackknife over individuals) and
\verb{_se_combined} (locus and individual uncertainty together). \strong{Report
\verb{_se_combined} for Ho, He and FIS.} Ar and privAr keep their locus-based
\verb{_se}, \verb{_lo} and \verb{_hi}, which already cover both sources. See "How
standard errors are calculated" below. Default \code{TRUE}, because
\verb{_se_combined} is the recommended standard error and the extra work is
small: about 30\% more run time (300,000 records and 60 individuals: 3.9
seconds became 5.2). \code{FALSE} skips it; then only the locus-based columns
exist, and they can be too small when individuals differ in inbreeding.}

\item{hierfstat_check}{If \code{TRUE} and the \code{hierfstat} package is installed,
also compute per-record Ho/Hs with \code{hierfstat::basic.stats()} and
allelic richness with \code{hierfstat::allelic.richness()}, and report the
largest difference from this package's values (with a warning if they
disagree). Default \code{FALSE}: the package tests already make this
comparison, and on large datasets the hierfstat calls are slow.}

\item{outdir}{Directory to write the result tables to as TSV files
(created if needed). Default \code{NULL}: write nothing.}

\item{stem}{Text in the output file names
(\verb{diversity_per_population.<stem>.tsv}). By default it comes from the
VCF's file name (\code{populations.haps.vcf} gives \code{"haps"}), so runs on the
haplotype and SNP VCFs can share \code{outdir} without overwriting each other.
Required when writing files from an already-read object, which has no
file name.}

\item{seed}{Random seed for the bootstrap. Default \code{NULL}: use R's current
random-number stream, so call \code{set.seed()} first for reproducible
intervals. A number makes the result reproducible on its own and leaves
your session's random-number stream as it was.}

\item{verbose}{Print progress messages. Default \code{TRUE}.}

\item{x, object}{A \code{raddiv_diversity} object, as returned by
\code{diversity_stats()}.}

\item{...}{Ignored.}

\item{details}{For \code{summary()}: \code{FALSE} (the default) prints a short view --
the main results as \code{estimate (SE)} with the one recommended standard
error for each statistic, what to take from this run, and a list of
checks. \code{TRUE} prints the full report: every uncertainty column, the
estimators, the filters and every explanation. Either way, the object
returned holds the tables the short view prints, in \verb{$tables} (see "Tables
for a paper").}
}
\value{
An object of class \code{raddiv_diversity}, a list of:
\describe{
\item{per_population}{Ho, He, FIS and \% polymorphic records per
population, each with its locus jackknife SE (\verb{_se}) and 95\% locus
bootstrap interval (\verb{_lo}, \verb{_hi}). With \code{se_individuals = TRUE} (the
default), also \verb{_se_ind} and \verb{_se_combined} for Ho, He and FIS.}
\item{richness}{Ar, privAr and priv_total per population, with the
number of records each rests on (\code{Ar_n}, \code{privAr_n}).}
\item{autosomal}{Per-sequenced-site Ho/He (\code{Ho_autosomal},
\code{He_autosomal}), with the \code{sites_used} and \code{variant_records} each
population was scaled by, and the same uncertainty columns as Ho and
He: \verb{_se}, \verb{_lo}, \verb{_hi} and, with \code{se_individuals = TRUE}, \verb{_se_ind}
and \verb{_se_combined} (e.g. \code{Ho_autosomal_se_combined}). A per-site value
is Ho (or He) times \code{variant_records / sites_used}, a fixed multiplier
for each population, so each of these is the matching Ho or He
column times the same multiplier. This treats \code{variant_records / sites_used} as fixed: it leaves out uncertainty in how many SNPs there
are per sequenced site, as the per-site value itself does. \code{NULL}
unless \code{sites} gave at least one population a plausible value, and
always \code{NULL} for a haplotype VCF.}
\item{estimator_comparison}{He and FIS from Nei & Chesser and from the
estimator behind Stacks' \code{Pi}, side by side.}
\item{he_difference}{For two populations with \code{nboot > 0}: the He
difference with its locus-bootstrap interval (not a test; see
\code{\link[=het_between_pops]{het_between_pops()}}). Otherwise \code{NULL}.}
\item{fis_by_call_rate}{A check for null alleles and allele dropout:
each population's records grouped by that population's call rate
(\verb{100\%}, \verb{90-99\%}, \verb{75-89\%}, \verb{<75\%}), with the number of records, He,
FIS and FIS's locus-jackknife SE in each group. FIS that rises as
call rate falls points to null alleles or dropout rather than
inbreeding (Gautier et al. 2013).}
\item{settings}{The settings and record counts of this run.}
}
Values are stored at full precision; \code{print()}, \code{summary()} and the TSV
files round them. With \code{outdir}, \code{per_population}, \code{richness} and
\code{autosomal} are written to \verb{diversity_<table>.<stem>.tsv}.
}
\description{
Computes Ho, He, FIS (Nei & Chesser 1983, ratio of sums), rarefied allelic
richness and rarefied private allelic richness from a Stacks 2
\code{populations} VCF and popmap, with a delete-one-locus jackknife standard
error and a bootstrap confidence interval over RAD loci. Printing the
result shows the main tables. \code{summary()} is short: the results as
\code{estimate (SE)} with the one recommended standard error for each statistic,
what to take from this run, and a list of checks. \code{summary(details = TRUE)}
is the full report, with every uncertainty column and notes on
interpretation. \code{outdir} also writes the tables to TSV files.
}
\details{
Run it twice: \code{populations.snps.vcf} gives Ho, He, \code{pct_poly} and the
per-sequenced-site values; \code{populations.haps.vcf} gives FIS, Ar and
privAr. Haplotype Ar and privAr depend on how many SNPs each RAD locus
holds, which changes with read length, enzyme and SNP calling. Compare them
only among populations analyzed together (the same run, filters and \code{g}):
which population is richer is reliable, but the size of the difference
also depends on SNPs per locus. The printed result says which numbers to
take from each file. See \code{vignette("rationale")}, section 3.

The SNP-VCF side was designed for biallelic SNPs, as Stacks writes them.
A VCF whose alleles are all single bases is treated as one site per record
even if a few SNPs are multi-allelic (as GATK or freebayes can emit), and
those sites are counted correctly; a VCF with any multi-base allele is
treated as haplotype data (see "Assumptions" in \code{\link[=read_stacks_vcf]{read_stacks_vcf()}}).
}
\section{How standard errors are calculated}{

A standard error (SE) says how much a number would change if the study were
repeated; the estimate plus or minus 1.96 SE is a 95\% confidence interval.
A population's He (or Ho, FIS, Ar) is uncertain for two reasons:
\itemize{
\item you typed \strong{some of the RAD loci} in the genome, and other loci would
give a slightly different value;
\item you caught \strong{some of the individuals} in the population, and other
individuals would give a slightly different value.
}

The columns measure these as follows:
\itemize{
\item \verb{_se}: a jackknife over RAD loci. Each RAD locus is left out in turn and
the statistic recomputed; how much those values spread gives the SE. It
holds the individuals fixed.
\item \verb{_lo}, \verb{_hi}: a bootstrap over RAD loci. Loci are drawn at random, with
replacement, thousands of times; the middle 95\% of the results is the
interval. It also holds the individuals fixed.
\item \verb{_se_ind} (with \code{se_individuals = TRUE}, the default): a jackknife over
individuals. Each individual is left out in turn. It holds the loci fixed.
\item \verb{_se_combined}: both sources together, \verb{sqrt(_se^2 + _se_ind^2)}.
}

Whole RAD loci, not single SNPs, are resampled, because SNPs on one RAD
locus are inherited together.

\strong{Which to report.} For \strong{Ho, He and FIS}, report \verb{_se_combined}. For
\strong{Ar and privAr}, report \verb{_se} (or \verb{_lo}, \verb{_hi}). The per-site Ho and He
(\code{Ho_autosomal}, \code{He_autosomal}) follow the same rule for Ho and He: report
\code{Ho_autosomal_se_combined} and \code{He_autosomal_se_combined}. The short
\code{summary()} shows only these recommended standard errors. This rule comes from
simulated populations with a known true value, where each simulated study
was repeated 300 times with new loci and new individuals, and we counted how
often the 95\% interval contained the truth (it should be about 95\%). With
the locus SE alone, Ho and FIS intervals contained the truth only 30-65\% of
the time in most settings once individuals differed in inbreeding: an inbred individual is
homozygous at many loci at once, so loci are not independent. With the
individual SE alone, He intervals contained it 47-86\% of the time, because
He depends mostly on which loci were typed. The combined SE contained it
90-99\% of the time for all three, often a little more than 95\% because some
noise is counted in both SEs. For Ar and privAr the locus SE alone did well
(88-96\%). A bootstrap over individuals is not offered for reporting:
drawing an individual twice makes the sample look less diverse than it is,
and its He intervals contained the truth under 10\% of the time. Details are
in \code{vignette("rationale")}, section 4, and \code{inst/sims/uncertainty_sources.R}.

\strong{What no SE covers.} Relatives in the sample (they bias the estimates
themselves; screen with \code{\link[=kinship_check]{kinship_check()}}), filtering choices and allele
dropout, and linkage between separate RAD loci.
}

\section{Tables for a paper}{

Put in the table exactly what the short \code{summary()} reports, and nothing
more. \code{summary(x)$tables} holds those tables as data frames, with the same
\code{estimate (SE)} text the summary prints: \code{results} (Ho, He and \code{pct_poly}
for a SNP VCF; FIS, Ar and privAr for a haplotype VCF) and, for a SNP VCF run
with \code{sites}, \code{per_site}. Join the two runs by population name, never by row
order:

\if{html}{\out{<div class="sourceCode">}}\preformatted{snp <- summary(res_snps)$tables
hap <- summary(res_haps)$tables
parts <- list(snp$results, snp$per_site,
              hap$results[names(hap$results) != "n"])
table1 <- Reduce(function(a, b) merge(a, b, by = "population", sort = FALSE),
                 Filter(Negate(is.null), parts))
table1 <- table1[match(snp$results$population, table1$population), ]
}\if{html}{\out{</div>}}

Leave out the other uncertainty columns (they are in the result tables and
the \code{outdir} files, for a supplement) and the counts behind a value (\code{Ar_n},
\code{privAr_n}, \code{priv_total}, \code{sites_used}); state those in the methods. Say in
the caption which VCF each column came from, the rarefaction size \code{g}, and
which standard error each column is. Add something to the table only when a
check in the summary said \code{look} and readers need the number to judge it.
See the README ("Make a table for a manuscript") and the workflow vignette
("A table for your manuscript").
}

\examples{
# A toy dataset shipped with the package: 80 RAD loci, 2 populations of 4
# and 3 individuals (real studies need far more individuals).
haps   <- system.file("extdata", "small.haps.vcf", package = "RADdiversity")
snps   <- system.file("extdata", "small.snps.vcf", package = "RADdiversity")
popmap <- system.file("extdata", "small_popmap.tsv", package = "RADdiversity")

# Haplotype VCF: FIS, allelic richness, private allelic richness.
# se_individuals = FALSE only because these toy populations have 3 and 4
# individuals, too few for the individual standard errors. Leave it at its
# default (TRUE) for real data.
set.seed(1)
res <- diversity_stats(haps, popmap, g = 4, nboot = 100, se_individuals = FALSE)
res                    # the main tables
res$per_population     # one table

# SNP VCF with each population's own sequenced-site count, read from Stacks'
# populations.sumstats_summary.tsv: Ho and He per sequenced site.
sumstats <- system.file("extdata", "populations.sumstats_summary.tsv",
                        package = "RADdiversity")
res_snps <- diversity_stats(snps, popmap, g = 4, nboot = 0, sites = sumstats,
                            se_individuals = FALSE, verbose = FALSE)
res_snps$autosomal

# A short summary: each estimate with its recommended SE, what to take from
# this run, and the checks.
summary(res_snps)

# The full report, with every uncertainty column and notes on each number:
summary(res_snps, details = TRUE)

# The tables the short summary prints, to build a table for a paper from:
summary(res_snps)$tables$results
}
\references{
Nei, M. & Chesser, R.K. (1983) Estimation of fixation indices and gene
diversities. \emph{Annals of Human Genetics} 47:253-259.
\doi{10.1111/j.1469-1809.1983.tb00993.x}

Weir, B.S. & Cockerham, C.C. (1984) Estimating F-statistics for the
analysis of population structure. \emph{Evolution} 38:1358-1370.
\doi{10.1111/j.1558-5646.1984.tb05657.x}

Hurlbert, S.H. (1971) The nonconcept of species diversity: a critique and
alternative parameters. \emph{Ecology} 52:577-586.

El Mousadik, A. & Petit, R.J. (1996) High level of genetic differentiation
for allelic richness among populations of the argan tree. \emph{Theoretical and
Applied Genetics} 92:832-839.

Kalinowski, S.T. (2004) Counting alleles with rarefaction: private alleles
and hierarchical sampling designs. \emph{Conservation Genetics} 5:539-543.
\doi{10.1023/B:COGE.0000041021.91777.1a}

Szpiech, Z.A., Jakobsson, M. & Rosenberg, N.A. (2008) ADZE: a rarefaction
approach for counting alleles private to combinations of populations.
\emph{Bioinformatics} 24:2498-2504.

Schmidt, T.L., Jasper, M.-E., Weeks, A.R. & Hoffmann, A.A. (2021) Unbiased
population heterozygosity estimates from genome-wide sequence data.
\emph{Methods in Ecology and Evolution} 12:1888-1898.
\doi{10.1111/2041-210X.13659}

Weir, B.S. (1996) \emph{Genetic Data Analysis II}. Sinauer, Sunderland, MA.
(Delete-one jackknife over loci.)

Nei, M. & Roychoudhury, A.K. (1974) Sampling variances of heterozygosity
and genetic distance. \emph{Genetics} 76:379-390. (Loci and individuals as two
sources of uncertainty.)

Shao, J. & Wu, C.F.J. (1989) A general theory for jackknife variance
estimation. \emph{Annals of Statistics} 17:1176-1197.
\doi{10.1214/aos/1176347263}

Owen, A.B. (2007) The pigeonhole bootstrap. \emph{Annals of Applied Statistics}
1:386-411. \doi{10.1214/07-AOAS122}

Owen, A.B. & Eckles, D. (2012) Bootstrapping data arrays of arbitrary
order. \emph{Annals of Applied Statistics} 6:895-927. (\code{boot = "both"}.)

Keenan, K., McGinnity, P., Cross, T.F., Crozier, W.W. & Prodohl, P.A.
(2013) diveRsity: an R package for the estimation and exploration of
population genetics parameters and their associated errors. \emph{Methods in
Ecology and Evolution} 4:782-788. (\code{boot = "individuals"}.)

Goudet, J. (2005) HIERFSTAT, a package for R to compute and test
hierarchical F-statistics. \emph{Molecular Ecology Notes} 5:184-186.
(\code{hierfstat_check}.)

Gautier, M., Gharbi, K., Cezard, T., et al. (2013) The effect of RAD
allele dropout on the estimation of genetic variation within and between
populations. \emph{Molecular Ecology} 22:3165-3178. (\code{fis_by_call_rate}.)
}
