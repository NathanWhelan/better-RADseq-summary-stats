% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diversity_table.R
\name{diversity_table}
\alias{diversity_table}
\alias{print.raddiv_table}
\title{A diversity table for a manuscript}
\usage{
diversity_table(snps = NULL, haps = NULL)

\method{print}{raddiv_table}(x, ...)
}
\arguments{
\item{snps}{The result of \code{\link[=diversity_stats]{diversity_stats()}} on the SNP VCF
(\code{populations.snps.vcf}). Optional.}

\item{haps}{The result of \code{\link[=diversity_stats]{diversity_stats()}} on the haplotype VCF
(\code{populations.haps.vcf}). Optional.}

\item{x}{A \code{raddiv_table}, as returned by \code{diversity_table()}.}

\item{...}{Ignored.}
}
\value{
A data frame (class \code{raddiv_table}) with one row per population, in
popmap order. Printing it shows the table and then the caption; the
caption is also in \code{attr(x, "caption")}. \code{write.csv()} and
\code{knitr::kable()} work on it as on any data frame.
}
\description{
Joins the results of the two \code{\link[=diversity_stats]{diversity_stats()}} runs into one table, one
row per population, and writes a caption to go with it. The table holds
exactly what the short \code{summary()} of each run shows: each statistic as
\code{estimate (SE)}, with the one standard error the package recommends.
}
\details{
\tabular{lll}{
   column \tab from \tab SE \cr
   \code{Ho (SE)}, \code{He (SE)} \tab SNP VCF \tab RAD loci and individuals \cr
   \code{Ho_autosomal (SE)}, \code{He_autosomal (SE)} (per sequenced site; only with \code{sites}) \tab SNP VCF \tab RAD loci and individuals \cr
   \code{Fis (SE)} \tab haplotype VCF \tab RAD loci and individuals \cr
   \code{Ar (SE)}, \verb{rarefied private alleles (SE)} \tab haplotype VCF \tab RAD loci only \cr
}


With \code{se_individuals = FALSE} in \code{\link[=diversity_stats]{diversity_stats()}}, the SEs count RAD
loci only, and the caption says so.

Other columns (the other SEs and intervals, \code{pct_poly}, \code{privAr}, and the
counts behind each value) stay in the results and the \code{outdir} files, for a
supplement. Report them only if readers need them to judge the table.

Give both runs, or only one: with only a SNP VCF (e.g. from ipyrad or
dDocent), pass \code{snps} alone.
}
\examples{
# Toy data: 2 populations of 4 and 3 individuals (far too few for a study).
snps   <- system.file("extdata", "small.snps.vcf", package = "RADdiversity")
haps   <- system.file("extdata", "small.haps.vcf", package = "RADdiversity")
popmap <- system.file("extdata", "small_popmap.tsv", package = "RADdiversity")
res_snps <- diversity_stats(snps, popmap, g = 4, nboot = 0, se_individuals = FALSE,
                            verbose = FALSE)
res_haps <- diversity_stats(haps, popmap, g = 4, nboot = 0, se_individuals = FALSE,
                            verbose = FALSE)
table1 <- diversity_table(res_snps, res_haps)
table1                        # the table, then its caption
attr(table1, "caption")       # the caption on its own
out <- tempfile(fileext = ".csv")
write.csv(table1, out, row.names = FALSE)
}
\seealso{
\code{\link[=diversity_stats]{diversity_stats()}}, "Tables for a paper".
}
