% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_loci.R
\name{filter_genotype_depth}
\alias{filter_genotype_depth}
\title{Set genotypes with too few or too many reads to missing}
\usage{
filter_genotype_depth(H, min_dp, max_dp = Inf, verbose = TRUE)
}
\arguments{
\item{H}{The object returned by \code{\link[=read_stacks_vcf]{read_stacks_vcf()}} (optionally filtered).
The read depths come from \code{H$depth}, which \code{\link[=read_stacks_vcf]{read_stacks_vcf()}} fills from
each call's \code{DP} (or the sum of its \code{AD}); it is \code{NULL} for a VCF with
neither, such as a Stacks haplotype VCF.}

\item{min_dp}{Calls with fewer reads than this are masked (e.g. \code{6}).}

\item{max_dp}{Calls with more reads than this are masked. Default \code{Inf}:
no upper limit.}

\item{verbose}{Print how many records were kept. Default \code{TRUE}.}
}
\value{
\code{H}, with the masked calls set to missing. Calls whose depth cannot
be read are left as they are (the message counts them).
}
\description{
Masks (sets to missing) every genotype call whose read depth is below
\code{min_dp} or above \code{max_dp}, whatever the genotype. Depth is the call's
\code{DP} value, or the sum of its \code{AD} (allele depths) where \code{DP} is absent.
}
\details{
Low depth is where heterozygotes are mistaken for homozygotes: with \code{d}
reads, a true heterozygote shows only one allele with probability
\code{2^(1 - d)} (25\% at 3 reads, 3\% at 6). Very high depth, far above the
typical depth in the dataset, is a sign of a collapsed paralog or repeat.

Because the rule ignores the genotype, heterozygous and homozygous calls at
the same depth are masked alike, and the message reports the share of each
that was masked. Prefer this to \code{\link[=filter_low_conf_alt]{filter_low_conf_alt()}} before computing
diversity statistics: that function masks only calls carrying an ALT
allele, which removes heterozygotes preferentially and biases Ho down and
FIS up.

Stacks' \code{populations --min-gt-depth} (Stacks 2.67 and later) applies the
same minimum-depth rule when the VCF is written. The haplotype VCF
(\code{populations.haps.vcf}) holds genotypes only, without depths, so this
filter needs the SNP VCF.
}
\examples{
vcf_lines <- c(
  "##fileformat=VCFv4.2",
  "#CHROM\tPOS\tID\tREF\tALT\tQUAL\tFILTER\tINFO\tFORMAT\tind1\tind2\tind3",
  "un\t1\tlocus_1\tA\tC\t.\tPASS\t.\tGT:DP:AD\t0/0:3:3,0\t0/1:12:6,6\t1/1:40:0,40"
)
vcf_file <- tempfile(fileext = ".vcf")
writeLines(vcf_lines, vcf_file)
H <- read_stacks_vcf(vcf_file, verbose = FALSE)
H_depth <- filter_genotype_depth(H, min_dp = 6, max_dp = 30)
H_depth$A1          # ind1 (3 reads) and ind3 (40 reads) are now missing
}
\references{
Rochette, N.C., Rivera-Colon, A.G. & Catchen, J.M. (2019) Stacks 2:
analytical methods for paired-end sequencing improve RADseq-based
population genomics. \emph{Molecular Ecology} 28:4737-4754.
}
