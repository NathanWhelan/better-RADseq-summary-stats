% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_loci.R
\name{filter_samples}
\alias{filter_samples}
\title{Keep only the individuals in a popmap}
\usage{
filter_samples(H, popmap, verbose = TRUE)
}
\arguments{
\item{H}{The object returned by \code{\link[=read_stacks_vcf]{read_stacks_vcf()}} (optionally filtered).}

\item{popmap}{A popmap file path or the list returned by \code{\link[=read_popmap]{read_popmap()}}.}

\item{verbose}{Print which samples were removed. Default \code{TRUE}.}
}
\value{
\code{H} with only the samples in \code{popmap}, in their original order.
}
\description{
Removes from \code{H} every sample that is not in \code{popmap}: its genotypes, read
depths and allele depths. Records are kept as they are.
}
\details{
Every analysis function uses only the popmap's individuals, but the
\verb{filter_*()} functions and \code{\link[=locus_allele_stats]{locus_allele_stats()}} work on EVERY sample in
\code{H}, and the \verb{write_*()} functions write every sample. A sample left out
of the popmap on purpose -- an outgroup, a failed library, a replicate --
still counts towards minor allele frequencies, call rates and
heterozygosity there, and still appears in exported files. Run
\code{filter_samples()} first to filter, and export, only the individuals you
analyse.

Removing samples can leave records with no genotyped individual, or with
only one allele among the remaining individuals; the message counts them.
Remove them with \code{\link[=filter_call_rate]{filter_call_rate()}} or \code{filter_mac(H, min_mac = 1)} if
needed.
}
\examples{
H <- read_stacks_vcf(system.file("extdata", "small.snps.vcf",
                                 package = "RADdiversity"), verbose = FALSE)
# A popmap that leaves out one individual:
pops <- list(popA = c("popA_1", "popA_2", "popA_3"),
             popB = c("popB_1", "popB_2", "popB_3"))
H_pop <- filter_samples(H, pops)
H_pop$samples
# Filters now see only these individuals:
H_pop <- filter_mac(H_pop, min_mac = 1)
}
