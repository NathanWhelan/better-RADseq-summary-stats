% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identity_disequilibrium.R
\name{identity_disequilibrium}
\alias{identity_disequilibrium}
\title{Identity disequilibrium (g2): do individuals differ in inbreeding?}
\usage{
identity_disequilibrium(
  H,
  pops,
  nboot = 1000L,
  nperm = 1000L,
  min_call = 0.9,
  seed = 2024
)
}
\arguments{
\item{H}{A list as returned by \code{\link[=read_stacks_vcf]{read_stacks_vcf()}} (optionally filtered).}

\item{pops}{A named list of sample-ID vectors, one per population, as
returned by \code{\link[=read_popmap]{read_popmap()}}.}

\item{nboot}{Bootstrap replicates over individuals for the CI. Default
\code{1000}; \code{0} for none.}

\item{nperm}{Permutations for the p-value. Default \code{1000}; \code{0} for none.}

\item{min_call}{Use a locus for a population only if at least this fraction
of that population's individuals is genotyped there. Default \code{0.9}.}

\item{seed}{Random seed, for reproducibility. The caller's own RNG state is
restored on exit.}
}
\value{
A data frame, one row per population: \code{population}, \code{n_ind},
\code{n_loci}, \code{g2}, \code{g2_se} (bootstrap SD), \code{g2_lo}, \code{g2_hi} (95\% bootstrap
interval) and \code{p_value} (one-sided, g2 > 0).
}
\description{
Estimates g2 (David et al. 2007) in each population: how much more often an
individual is heterozygous at two loci at once than two different
individuals are. g2 is 0 when individuals do not differ in inbreeding and
positive when they do.
}
\details{
\strong{Why it matters for this package.} Standard errors and intervals that
resample loci (\code{\link[=diversity_stats]{diversity_stats()}}) hold the sampled individuals fixed.
With many loci that is also enough for population-level inference, as
long as individuals are exchangeable (Nei & Roychoudhury 1974; Nei 1978).
When individuals differ in inbreeding -- or include relatives --
heterozygosity is correlated across loci within an individual and the
locus-based intervals are too narrow. A g2 confidence interval that
excludes 0 is the signal to use the individual-jackknife standard errors
of \code{\link[=diversity_stats]{diversity_stats()}} (\code{se_individuals = TRUE}) and the individual-level
tests of \code{\link[=het_between_pops]{het_between_pops()}}.

\strong{Estimator.} From the definition, using every pair of distinct loci
typed in the same individual (numerator) and in two different individuals
(denominator); with no missing data this is the closed form of Hoffman et
al. (2014). The confidence interval is a bootstrap over individuals; the
p-value comes from permutations that shuffle heterozygosity among each
locus's typed individuals (\code{(exceedances + 1) / (nperm + 1)}). Any marker
type works -- SNPs or multi-allelic RAD haplotypes. Pooling structured
populations inflates g2 (Wahlund effect), hence one estimate per
population.
}
\examples{
H <- read_stacks_vcf(system.file("extdata", "small.haps.vcf",
                                 package = "RADdiversity"), verbose = FALSE)
pops <- read_popmap(system.file("extdata", "small_popmap.tsv",
                                package = "RADdiversity"), H$samples)
identity_disequilibrium(H, pops, nboot = 200, nperm = 200, min_call = 0.5)
}
\references{
David, P., Pujol, B., Viard, F., Castella, V. & Goudet, J. (2007) Reliable
selfing rate estimates from imperfect population genetic data. \emph{Molecular
Ecology} 16:2474-2487.

Hoffman, J.I., Simpson, F., David, P., Rijks, J.M., Kuiken, T., Thorne,
M.A.S., Lacy, R.C. & Dasmahapatra, K.K. (2014) High-throughput sequencing
reveals inbreeding depression in a natural population. \emph{PNAS}
111:3775-3780.

Nei, M. (1978) Estimation of average heterozygosity and genetic distance
from a small number of individuals. \emph{Genetics} 89:583-590.

Nei, M. & Roychoudhury, A.K. (1974) Sampling variances of heterozygosity
and genetic distance. \emph{Genetics} 76:379-390.
}
