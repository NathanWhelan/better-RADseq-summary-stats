% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isolation_by_distance.R
\name{isolation_by_distance}
\alias{isolation_by_distance}
\alias{print.raddiv_ibd}
\alias{summary.raddiv_ibd}
\alias{plot.raddiv_ibd}
\title{Isolation by distance: do populations farther apart differ more?}
\usage{
isolation_by_distance(
  genetic,
  distances,
  habitat,
  stat = "FST",
  method = "pearson",
  nperm = 9999L,
  exclude = NULL,
  seed = NULL,
  verbose = TRUE
)

\method{print}{raddiv_ibd}(x, ...)

\method{summary}{raddiv_ibd}(object, details = FALSE, ...)

\method{plot}{raddiv_ibd}(x, ...)
}
\arguments{
\item{genetic}{The result of \code{\link[=differentiation_stats]{differentiation_stats()}}, or a square matrix
of pairwise values with population names as row and column names (for
example FST from another program). One half of the matrix may be blank
(\code{NA}), as many programs write it; it is filled from the other half. A
data frame with the names in its first column also works.}

\item{distances}{The distances between populations: a file path, data
frame, matrix or \code{dist} object, in either layout that \code{\link[=read_distances]{read_distances()}}
accepts. Population names must match those in \code{genetic}; their order does
not matter.}

\item{habitat}{Required. \code{"1D"} for populations along a line -- a river,
stream, coastline or other narrow strip -- where genetic values are
compared with the distance. \code{"2D"} for populations spread over an area,
where they are compared with the natural log of the distance (every
distance must then be above 0).}

\item{stat}{\code{"FST"} (the default), used as FST/(1 - FST), or \code{"D"} (Jost's
D), used as it is.}

\item{method}{\code{"pearson"} (the default) for the correlation of the values,
or \code{"spearman"} for the correlation of their ranks, which is less swayed
by a few extreme pairs. The slope always uses the values.}

\item{nperm}{Random orderings of the populations when there are 8 or more.
Default \code{9999}. With 7 or fewer every ordering is used and \code{nperm} is
ignored.}

\item{exclude}{Names of populations to leave out, for example one far from
the others, to see how much it drives the result.}

\item{seed}{\code{NULL} (the default: use the session's random numbers; call
\code{set.seed()} first for reproducible results), or a whole number, used
for this call only.}

\item{verbose}{Print progress messages. Default \code{TRUE}.}

\item{x, object}{A \code{raddiv_ibd} object, as returned by
\code{isolation_by_distance()}.}

\item{...}{For \code{plot()}: graphical settings passed to \code{\link[graphics:plot.default]{graphics::plot()}},
such as \code{xlab = "River km"} or \code{main = ""}. Otherwise ignored.}

\item{details}{For \code{summary()}: \code{FALSE} (the default) prints a short view --
the result, what to report and a list of checks. \code{TRUE} adds every pair
and the notes on the method. Either way, the object returned holds the
result table in \verb{$tables$results}.}
}
\value{
An object of class \code{raddiv_ibd}, a list of:
\describe{
\item{result}{One row: what was compared (\code{genetic}, \code{geographic},
\code{habitat}), \code{populations}, \code{pairs}, \code{method}, \code{mantel_r}, \code{p_value},
\code{orderings} (how many were used), \code{exact} (\code{TRUE} when every ordering
was used), \code{slope} and \code{intercept}.}
\item{pairs}{One row per pair of populations: \code{pop1}, \code{pop2},
\code{distance}, \code{log_distance} (2-D only), and \code{FST} and \code{FST_linearized}
(= FST/(1 - FST)), or \code{D}.}
\item{settings}{The settings of this run, the populations used and
those left out.}
}
\code{summary()} shows the result, what to report and a list of checks;
\code{summary(x)$tables$results} is that table as a data frame, for a paper.
\code{plot()} draws the pairs and the fitted line.
}
\description{
Tests whether genetic differentiation between populations grows with the
geographic distance between them, with a Mantel test (Mantel 1967), and
gives the slope of that relationship (Rousset 1997). It uses the pairwise
FST or Jost's D from \code{\link[=differentiation_stats]{differentiation_stats()}} and a table of distances
read by \code{\link[=read_distances]{read_distances()}}.
}
\details{
\strong{What is compared.} With \code{stat = "FST"} (the default), each pair's FST
is used as FST/(1 - FST), as Rousset (1997) showed. In a 1-D habitat it is
compared with the distance, in a 2-D habitat with the natural log of the
distance. Under isolation by distance both relationships are close to
straight lines, and the slope estimates dispersal: 1/slope estimates
4 x density x sigma^2 in 1-D and 4 x pi x density x sigma^2 in 2-D, where
density is individuals per unit of length or area and sigma^2 is the mean
squared distance between parent and offspring along one axis. These hold
best for pairs farther apart than sigma, in populations near equilibrium.

\strong{Jost's D} (\code{stat = "D"}) is used as it is. For a pair of populations
with similar diversity, D equals FST/(1 - FST) times Hs/(1 - Hs), so D
already has the straight-line shape; D/(1 - D) would bend it and is not
offered. The slope of D is reported to describe the pattern, but only the
FST slope estimates dispersal (see \code{vignette("rationale")}).

\strong{The test.} \code{r} is the correlation between the genetic and the geographic
values over all pairs of populations. Pairs share populations, so \code{r}
cannot be tested as an ordinary correlation. Instead, the populations are
shuffled among the geographic positions and \code{r} recomputed each time; \code{p}
is the share of orderings giving an \code{r} at least as large as the data's
(one-sided: isolation by distance predicts a positive \code{r}). With 7 or fewer
populations every ordering is tried (at most 5,040), so \code{p} is exact and
the same on every run. With more, \code{nperm} random orderings are used and
p = (b + 1)/(nperm + 1), where b counts those at least as large (Phipson &
Smyth 2010). With few populations the test has little power: with 3
populations \code{p} can never be below 1/6.

The simple Mantel test is valid for isolation by distance (Guillot &
Rousset 2013). Partial Mantel tests, which try to control for a third
distance matrix, are not valid for spatially structured data and are not
offered.

\strong{The slope} is an ordinary least-squares line through all pairs, a point
estimate with no standard error here. Genepop (Rousset 2008) gives a
bootstrap interval for it.

\strong{Negative values.} A pairwise FST or D estimate can be slightly below 0
when two populations barely differ. Such values are kept as they are:
setting them to 0 would bias the slope.
}
\examples{
# A small made-up example: 4 populations along a river.
fst <- matrix(c(0,    0.02, 0.05, 0.08,
                0.02, 0,    0.03, 0.06,
                0.05, 0.03, 0,    0.02,
                0.08, 0.06, 0.02, 0), 4,
              dimnames = list(c("a", "b", "c", "d"), c("a", "b", "c", "d")))
km <- data.frame(pop1 = c("a", "a", "a", "b", "b", "c"),
                 pop2 = c("b", "c", "d", "c", "d", "d"),
                 distance = c(10, 25, 40, 15, 30, 15))
isolation_by_distance(fst, km, habitat = "1D", verbose = FALSE)

# From VCF to test, with the example data: six sites along a river.
vcf    <- system.file("extdata", "ibd_example.snps.vcf.gz", package = "RADdiversity")
popmap <- system.file("extdata", "ibd_example_popmap.tsv", package = "RADdiversity")
km     <- system.file("extdata", "ibd_example_distances.csv", package = "RADdiversity")
dif <- differentiation_stats(vcf, popmap, nboot = 0, beta = FALSE, verbose = FALSE)
ibd <- isolation_by_distance(dif, km, habitat = "1D")
summary(ibd)
plot(ibd, xlab = "River km")
isolation_by_distance(dif, km, habitat = "1D", stat = "D", verbose = FALSE)
}
\references{
Wright, S. (1943) Isolation by distance. \emph{Genetics} 28:114-138.
\doi{10.1093/genetics/28.2.114}

Mantel, N. (1967) The detection of disease clustering and a generalized
regression approach. \emph{Cancer Research} 27:209-220.

Rousset, F. (1997) Genetic differentiation and estimation of gene flow from
F-statistics under isolation by distance. \emph{Genetics} 145:1219-1228.
\doi{10.1093/genetics/145.4.1219}

Rousset, F. (2008) genepop'007: a complete re-implementation of the genepop
software for Windows and Linux. \emph{Molecular Ecology Resources} 8:103-106.
\doi{10.1111/j.1471-8286.2007.01931.x}

Phipson, B. & Smyth, G.K. (2010) Permutation P-values should never be zero:
calculating exact P-values when permutations are randomly drawn.
\emph{Statistical Applications in Genetics and Molecular Biology} 9:39.
\doi{10.2202/1544-6115.1585}

Guillot, G. & Rousset, F. (2013) Dismantling the Mantel tests. \emph{Methods in
Ecology and Evolution} 4:336-344. \doi{10.1111/2041-210x.12018}

Jost, L. (2008) GST and its relatives do not measure differentiation.
\emph{Molecular Ecology} 17:4015-4026. \doi{10.1111/j.1365-294X.2008.03887.x}
}
\seealso{
\code{\link[=read_distances]{read_distances()}} for the distance file, \code{\link[=differentiation_stats]{differentiation_stats()}}
for FST and D.
}
