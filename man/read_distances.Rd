% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isolation_by_distance.R
\name{read_distances}
\alias{read_distances}
\title{Read distances between populations}
\usage{
read_distances(distances, verbose = TRUE)
}
\arguments{
\item{distances}{A file path (CSV or tab-separated), a data frame, a
matrix with population names as row and column names, or a \code{dist}
object with labels.}

\item{verbose}{Print a line saying what was read. Default \code{TRUE}.}
}
\value{
A symmetric numeric matrix with population names as row and column
names and 0 on the diagonal. A pair with no distance in the input is
\code{NA} (and a message says so); \code{\link[=isolation_by_distance]{isolation_by_distance()}} stops if it needs
that pair.
}
\description{
Reads the geographic distances for \code{\link[=isolation_by_distance]{isolation_by_distance()}} and returns
them as a symmetric matrix named by population. Print the result to check
that your file was read the way you meant.
}
\details{
Two layouts are accepted, as a CSV file (comma-separated) or a
tab-separated file. Population names must be spelled exactly as in the
popmap. Their order does not matter: rows and columns are matched by name.

\strong{A square matrix}, with population names across the first row and down
the first column. The top-left cell may be blank or missing. One half of the
matrix may be left blank; it is filled from the other half. If both halves
are filled, they must agree.

\if{html}{\out{<div class="sourceCode">}}\preformatted{,site1,site2,site3
site1,0,15,27
site2,,0,12
site3,,,0
}\if{html}{\out{</div>}}

\strong{A list of pairs}, one row per pair of populations: two names and the
distance. The header row is optional, and the order of the two names does
not matter.

\if{html}{\out{<div class="sourceCode">}}\preformatted{pop1,pop2,distance
site1,site2,15
site1,site3,27
site2,site3,12
}\if{html}{\out{</div>}}

Use any unit (km, m, river km); the slope from \code{\link[=isolation_by_distance]{isolation_by_distance()}} is
per unit of your distances. Use the distance the organisms actually travel:
along the river for stream animals, for example, not a straight line.
}
\examples{
km <- system.file("extdata", "ibd_example_distances.csv", package = "RADdiversity")
read_distances(km)

# The same distances as a list of pairs
pairs <- data.frame(pop1 = c("a", "a", "b"), pop2 = c("b", "c", "c"),
                    distance = c(15, 27, 12))
read_distances(pairs)
}
\seealso{
\code{\link[=isolation_by_distance]{isolation_by_distance()}}
}
