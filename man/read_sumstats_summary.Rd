% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_sumstats_summary.R
\name{read_sumstats_summary}
\alias{read_sumstats_summary}
\title{Read a Stacks \code{populations.sumstats_summary.tsv} file}
\usage{
read_sumstats_summary(path)
}
\arguments{
\item{path}{Path to \code{populations.sumstats_summary.tsv}.}
}
\value{
A list with elements \code{variant_positions} and \code{all_positions},
each a data frame with one row per population and columns:
\code{population}, \code{private}, (in \code{all_positions} only:) \code{sites},
\code{variant_sites}, \code{polymorphic_sites}, \code{pct_polymorphic_loci}, then for
each of \code{num_indv}, \code{p}, \code{obs_het}, \code{obs_hom}, \code{exp_het}, \code{exp_hom},
\code{pi}, \code{fis}: the value itself plus \verb{<name>_var} and \verb{<name>_se}.
}
\description{
Parses both blocks of Stacks' \code{populations.sumstats_summary.tsv}
("Variant positions" and "All positions (variant and fixed)") into one
data frame per block, one row per population.
}
\details{
This package's own Ho, He and FIS estimators deliberately differ from
Stacks' -- see \code{vignette("rationale")} ("Formulas"): Nei & Chesser's He instead of
Stacks' \code{Pi} (which assumes FIS = 0), and FIS as a ratio of sums instead
of Stacks' mean of per-locus ratios. \strong{Nothing in this package uses the
values this function returns as an estimate of Ho, He, pi or FIS} --
\code{\link[=diversity_stats]{diversity_stats()}} computes all of those itself. This function exists so
the one column here that is a plain tally rather than an estimator --
\code{sites} (how many nucleotide positions Stacks retained for that
population, in the "All positions" block) -- can be read by name and
handed to \code{\link[=diversity_stats]{diversity_stats()}}'s \code{sites} argument instead of retyped by
hand. \code{sites} differs slightly per population for the same reason a locus
can be missing from one population and not another.
}
\examples{
triple_hdr <- paste(
  "Num_Indv", "Var", "StdErr", "P", "Var", "StdErr", "Obs_Het", "Var",
  "StdErr", "Obs_Hom", "Var", "StdErr", "Exp_Het", "Var", "StdErr",
  "Exp_Hom", "Var", "StdErr", "Pi", "Var", "StdErr", "Fis", "Var",
  "StdErr", sep = "\t")
lines <- c(
  "# Variant positions",
  paste0("# Pop ID\tPrivate\t", triple_hdr),
  paste("popA", 0, 10, 0, 0, 0.9, 0, 0, 0.2, 0, 0, 0.8, 0, 0, 0.18, 0, 0,
        0.82, 0, 0, 0.19, 0, 0, -0.05, 0, 0, sep = "\t"),
  "# All positions (variant and fixed)",
  paste0("# Pop ID\tPrivate\tSites\tVariant_Sites\tPolymorphic_Sites\t",
         "\%Polymorphic_Loci\t", triple_hdr),
  paste("popA", 0, 100000, 10, 4, 40, 10, 0, 0, 0.9, 0, 0, 0.00002, 0, 0,
        0.99998, 0, 0, 0.000018, 0, 0, 0.999982, 0, 0, 0.000019, 0, 0,
        -0.05, 0, 0, sep = "\t")
)
f <- tempfile(fileext = ".tsv")
writeLines(lines, f)
read_sumstats_summary(f)$all_positions$sites  # 1e5
}
