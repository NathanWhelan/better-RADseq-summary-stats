% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_formats.R
\name{write_fstat}
\alias{write_fstat}
\title{Write a (filtered) H as an FSTAT input file}
\usage{
write_fstat(H, path, pops, verbose = TRUE)
}
\arguments{
\item{H}{A list as returned by \code{\link[=read_stacks_vcf]{read_stacks_vcf()}}.}

\item{path}{Output file path.}

\item{pops}{A named list of sample-ID vectors (from \code{\link[=read_popmap]{read_popmap()}}).
Required -- FSTAT's first data column is always the population number.}

\item{verbose}{Print a short summary once written. Default \code{TRUE}.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes the FSTAT text format used by FSTAT itself and by the \code{hierfstat}
R package. When \code{hierfstat} is installed, this delegates to its own
\code{write.fstat()} (reusing the exact genotype-coding data frame shape
\code{\link[=diversity_stats]{diversity_stats()}} already builds internally); otherwise an internal
fallback writes the format directly. Both confirmed against \code{hierfstat}'s
documented FSTAT format description.
}
\details{
When \code{hierfstat} is installed, this relies on its own
\code{write.fstat()}, which has a known limitation of its own with a
dataset of exactly ONE locus (it errors rather than writes a
one-column file) -- not something a real RADseq dataset (always many
loci) will ever run into, but worth knowing if you're experimenting
with a tiny hand-built \code{H} for testing.
}
\examples{
H <- list(
  A1 = matrix(c(1L, 2L, 1L, 1L), nrow = 2),
  A2 = matrix(c(1L, 2L, 1L, 2L), nrow = 2),
  locus = c("locus_1", "locus_2"), locus_raw = c("locus_1", "locus_2"),
  n_alleles = c(2L, 2L), samples = c("a1", "a2")
)
pops <- list(popA = "a1", popB = "a2")
out <- tempfile()
write_fstat(H, out, pops = pops, verbose = FALSE)
cat(readLines(out), sep = "\n")
}
