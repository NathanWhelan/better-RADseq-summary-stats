% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_formats.R
\name{write_radpainter}
\alias{write_radpainter}
\title{Write a (filtered) H as a RADpainter / fineRADstructure input file}
\usage{
write_radpainter(H, path, verbose = TRUE)
}
\arguments{
\item{H}{The object returned by \code{\link[=read_stacks_vcf]{read_stacks_vcf()}}, ideally from a
haplotype VCF.}

\item{path}{Output file path. A name ending in \code{.gz} writes a
gzip-compressed file.}

\item{verbose}{Print a short summary once written. Default \code{TRUE}.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes the haplotype input format of fineRADstructure's \code{RADpainter}: a
header line of sample names, then one line per RAD locus with each
individual's two haplotypes. The layout follows the fineRADstructure
project's \code{hapsFromVCF.cpp} and the community \code{finerad_input.py} script:
no blank column before the sample names, and a missing genotype written
as an EMPTY field (nothing between its two tabs).
}
\details{
RADpainter uses haplotypes, so this is meant for a haplotype \code{H} (one
multi-allelic record per RAD tag, from \code{populations.haps.vcf}).
}
\examples{
H <- read_stacks_vcf(system.file("extdata", "small.haps.vcf",
                                 package = "RADdiversity"), verbose = FALSE)
out <- tempfile()
write_radpainter(H, out)
readLines(out)[1:2]
}
\references{
Malinsky, M., Trucchi, E., Lawson, D.J. & Falush, D. (2018) RADpainter and
fineRADstructure: population inference from RADseq data. \emph{Molecular Biology
and Evolution} 35:1284-1290.
}
